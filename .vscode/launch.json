{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/out/install/debug/bin/tupa_engine",
            "args": [],
            "cwd": "${workspaceFolder}/out",
            "preLaunchTask": "install",
            "env": {
                "LD_LIBRARY_PATH": "${workspaceFolder}/out/install/debug/lib"
            },
            "reverseDebugging": true
        }
    ]
}